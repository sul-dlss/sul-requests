<%= form_with(model: request, data: { turbo: true, controller: 'appointment-select-submit', action: 'appointment-select-submit#showSubmitsWith turbo:submit-end->appointment-select-submit#hideSubmitsWith'}) do |f| %>
  <%= hidden_field_tag 'sidebar', true %>
  <%= render 'aeon_appointments/dropdown', appointments: selectable_appointments, reading_room_id: request.reading_room.id %>
<% end %>
