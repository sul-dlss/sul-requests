<div class="my-4">
  <h3 class="text-digital-red"><i class="bi bi-pin-angle-fill me-2"></i>Saved for later</h3>
  <p>You <%= submitted_requests? ? 'also saved' : 'saved' %> <%= pluralize(saved_count, 'item') %> for later. <%= digital? ? 'Complete' : 'Schedule' %> them anytime at Requests &gt; <%= link_to 'Saved for later', aeon_requests_path(kind: 'saved_for_later'), class: 'su-underline', data: { turbo: false } %>.</p>
</div>
