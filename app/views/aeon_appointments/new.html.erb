<turbo-frame id="appointment-form">
  <%= render Aeon::AppointmentHeadingComponent.new(reading_room: @reading_room) do %>Create new appointment<% end %>
  <% if @appointment.reading_room_id %>
    <%= render 'form' %>
  <% else %>
    <%= render 'choose_reading_room' %>
  <% end %>
</turbo-frame>
