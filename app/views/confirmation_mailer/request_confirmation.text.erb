On <%= l @request.created_at.to_date, format: :long %>, you requested the following:

<%= @request.item_title %>

Item(s) requested:
  <%= @request.holdings.map(&:callnumber).join("\n  ") %>
  <%= @request.ad_hoc_items.join("\n  ") if @request.ad_hoc_items.present? %>

<%= @request.data_to_email_s %>

<%= t('approval_status.spec_coll.extra_note') %>
<% if @request.needed_date.present? %>
<%= "If your request is approved, we'll have it ready when you visit on: #{l @request.needed_date, format: :long}" %>
<% end %>

Check the status of your request at <%= @status_url %>

Questions about your request?
Contact:
<%= @contact_info %>
