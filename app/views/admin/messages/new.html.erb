<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
		<title>Message Input Form</title>
	</head>
	<body>
		<%= render :partial => 'header' %> 
		<h1>Message Input Form</h1>
		<% unless flash[:notice].blank?%>
			<div id="notification"><%= flash[:notice] %></div>
		<% end %>
		<% if @message.errors.any? %>
		  <ul>
		    <% @message.errors.full_messages.each do |msg| %>
		      <li><%= msg %></li>
		    <% end %>
		  </ul>
		<% end %>
		<%= form_for ( [:admin, @message ]) do |f| %>
		
		<p>
		This form is for adding messages that appear on the confirmation page 
		when requests are submitted.
		Note that message keys must match strings used in Symphony or
		in the request application itself. Message text can be anything 
		you want.
		
		</p>

		<table>
			<tr>
				<td nowrap valign="top" align="right">Message Key:</td>
			 	<td valign="top"><%= f.text_field :msg_number %></td>
			 <tr>
			 <tr>
				<td nowrap valign="top" align="right">Message Text:</td>
				 <td valign="top"><%= f.text_area :msg_text, :cols => 40, :rows => 3 %></td>
			</tr>	
			
			 <tr>
				<td nowrap valign="top" align="right">Comments:</td>
				 <td valign="top" colspan=2><%= f.text_area :comments, :cols => 40, :rows => 10 %></td>
			</tr>						
				
			<tr>		
				<td colspan=2><%= submit_tag "Save"  %></td>
			</tr>
			</table>

		<% end %>			
		
		
		
	</body>
</html>
