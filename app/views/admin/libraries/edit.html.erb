<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
		<title>Edit Library</title>
	</head>
	<body>
		<%= render :partial => 'header' %> 
		
		<h1>Library Edit Form</h1>
		<% unless flash[:notice].blank?%>
			<div id="notification"><%= flash[:notice] %></div>
		<% end %>
		
		<% if @library.errors.any? %>
		  <ul>
		    <% @library.errors.full_messages.each do |msg| %>
		      <li><%= msg %></li>
		    <% end %>
		  </ul>
		<% end %>
		
		<%= form_for ( [:admin, @library] ) do |f| %>
						
		<table>
			<tr>
				<td nowrap valign="top" align="right">Library Code:</td>
			 	<td valign="top"><%= f.text_field :lib_code %></td>
			 <tr>
			 <tr>
				<td nowrap valign="top" align="right">Library Description:</td>
				 <td valign="top"><%= f.text_field :lib_descrip %></td>
			</tr>
			 
			 <tr>		
				<td colspan=2><%= f.submit 'Save'  %></td>
			</tr>

			 </table>
			 
			
		<% end %>				 
	</body>
</html>
