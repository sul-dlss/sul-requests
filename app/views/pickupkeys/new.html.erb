<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
		<title>Pickup Key Input Form</title>
	</head>
	<body>
		<%= render :partial => 'header' %> 
		<h1>Pickup Key Input Form</h1>
		<% unless flash[:notice].blank?%>
			<div id="notification"><%= flash[:notice] %></div>
		<% end %>
		<%= error_messages_for 'pickupkey' %>
		
		Use this form to add a new pickup library key, which can be used to define pickup libraries
		for various requests. In general, a key should be a library code used in Symphony
		(e.g., "HOOVER", "LAW"), the string that comes after "PAGE-" in SAL3 "PAGE-XXX" locations 
		(e.g., "MU" from "PAGE-MU"), or the string that comes after "SAL3-T0-" in request types like
		"SAL3-TO-BR" (for which the pickup key would be "BR").
		<P></P>
		
		<% form_for (:pickupkey, @pickupkey, :url => { :action => 'create'}) do |f| %>
					
		<table>
			<tr>
				<td nowrap valign="top" align="right">Pickup Library Key:</td>
			 	<td valign="top"><%= f.text_field :pickup_key %></td>
			 <tr>
			 <tr>
				<td nowrap valign="top" align="right">Description:</td>
				 <td valign="top"><%= f.text_field :pickup_descrip %></td>
			</tr>
			
				
			<tr>		
				<td colspan=2><%= submit_tag "Save"  %></td>
			</tr>
			</table>

		<% end %>			
		
		
		
	</body>
</html>
