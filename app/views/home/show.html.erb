<h1>Request management</h1>

<div>
  <h4>Administration and mediation tasks for page, scan, and hold/recall requests from SearchWorks.</h4>
  Contact <%= mail_to Settings.contact_email %> if you have questions about your access to these options.
</div>

<% if can? :manage, Request.new %>
  <%= render 'administration' %>
  <%= render 'mediation' %>
<% else %>
  <h4 class="access-error">You do not have access</h4>
<% end %>
