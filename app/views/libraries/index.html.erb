<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
		<title>Libraries</title>
	</head>
	<body>
		<%= render :partial => 'header' %> 
		<h1>Pickup Libraries</h1>
		
		Listed below are libraries that can be included in a list of pickup libraries. The "Library"
		column contains the library code used in Symphony. The "Description" contains the name of the
		library as it appears in a drop-down list. 

<table>
  <tr>
    <th>Library</th>
	<th>Description</th>
  </tr>

<% for library in @libraries %>
  <tr>
    <td><%=h library.lib_code %></td>
    <td><%=h library.lib_descrip %></td>
    <td><%= link_to 'Edit', edit_library_path(library) %></td>
    <td><%= link_to 'Delete', library, :confirm => 'Are you sure you want to delete this record?', 
         :method => :delete %></td>
      </tr>
<% end %>
</table>

	</body>
</html>
