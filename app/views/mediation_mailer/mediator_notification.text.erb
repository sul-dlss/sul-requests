On <%= l @request.created_at, format: :short %>, <%= @request.user.to_email_string %> requested the following:

<%= @request.item_title %>

Item(s) requested:
  <%= @request.holdings.map(&:callnumber).join("\n  ") %>
  <%= @request.ad_hoc_items.join("\n  ") if @request.ad_hoc_items.present? %>

<%= @request.data_to_email_s %>

<% if @request.needed_date.present? %>
<%= "#{@request.class.human_attribute_name(:needed_date)}: #{l @request.needed_date, format: :quick}" %>
<% end %>

Login to view and approve the request at <%= @mediation_url %>
