<div id="scan-or-deliver" data-scheduler-lookup-url='<%= paging_schedule_path(origin: current_request.paging_origin_library) %>'>
  <%= render RequestAndPickupButtonComponent.new(current_request:) %>

  <%= render ScanToPdfButtonComponent.new(current_request:) %>
</div>
