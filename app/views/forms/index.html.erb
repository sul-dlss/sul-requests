<h1>Forms</h1>
<p>Forms to use for requests:</p>

<% @forms.each do |f| %>
<li><%= link_to f.title, {:action => 'show', :id => f.id} %>
<%= link_to 'Edit', :action => 'edit', :id => f.id %> 
<%= link_to 'Delete', :action => 'delete', :id => f.id %> 
</li>
<% end %>


